1. git remote add origin <ssh - link>
this command connects your local repo with your remote repo.your remote repo
will be represented by name "origin"

2. git remote -v
this is command displays all the remotes connected to your local repo.

3.  ssh-keygen -t rsa -b 4096 -C "manishrazasah22@gmail.com"
it generates a new ssh - key for your local(laptop)

4. git add .
it adds all the changes  of the repo to git

5. git commit -m "<commit -message>"
commits the changes added to git

6. git push origin <remote_branch_name>